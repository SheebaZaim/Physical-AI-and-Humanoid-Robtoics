---
title: 'User Personalization & Translation Features'
sidebar_label: 'Personalization'
slug: '/personalization'
---

import React from 'react'

export const frontmatter = {
  title: 'User Personalization & Translation Features',
  sidebar_label: 'Personalization',
  slug: '/personalization'
}

// LanguageSelector component: minimal, accessible, and client-side only.
// Place this at the top of each chapter .mdx (or import it from a shared component file).

export function LanguageSelector({ defaultLang = 'en' }) {
  const [lang, setLang] = React.useState(defaultLang)

  React.useEffect(() => {
    // keep selection in localStorage so user choice persists across pages
    try { localStorage.setItem('book_lang', lang) } catch (e) {}
  }, [lang])

  return (
    <div className="language-selector" aria-label="Choose language" style={{ marginBottom: '1rem' }}>
      <div style={{ display: 'flex', gap: '0.5rem', flexWrap: 'wrap' }}>
        <button onClick={() => setLang('en')} aria-pressed={lang === 'en'}>English</button>
        <button onClick={() => setLang('ur')} aria-pressed={lang === 'ur'}>اردو</button>
        <button onClick={() => setLang('ru')} aria-pressed={lang === 'ru'}>Roman Urdu</button>
        <button onClick={() => setLang('ar')} aria-pressed={lang === 'ar'}>العربية</button>
        <button onClick={() => setLang('de')} aria-pressed={lang === 'de'}>Deutsch</button>
      </div>

      <style>{`
        .language-selector button { padding: 0.45rem 0.7rem; border-radius: 6px; border: 1px solid rgba(0,0,0,0.08); background: transparent; cursor: pointer; }
        .language-selector button[aria-pressed="true"] { box-shadow: 0 0 0 3px rgba(0,0,0,0.04) inset; font-weight: 600; }
      `}</style>

      {/* Hidden inputs to allow CSS-only targeting if desired */}
      <input type="hidden" id="selected-lang" value={lang} />

      {/* Render translations container statefully */}
      <div>
        {/* The translation blocks below are always present in the .mdx (for RAG / offline retrieval),
            but they include a data-language attribute and are hidden unless selected. */}
        <div data-language="en" style={{ display: lang === 'en' ? 'block' : 'none' }}>
          {/* English content will appear here (can duplicate main content or provide summary). */}
        </div>
        <div data-language="ur" style={{ display: lang === 'ur' ? 'block' : 'none' }}>
          {/* Urdu translation block */}
        </div>
        <div data-language="ru" style={{ display: lang === 'ru' ? 'block' : 'none' }}>
          {/* Roman Urdu translation block */}
        </div>
        <div data-language="ar" style={{ display: lang === 'ar' ? 'block' : 'none' }}>
          {/* Arabic translation block */}
        </div>
        <div data-language="de" style={{ display: lang === 'de' ? 'block' : 'none' }}>
          {/* German translation block */}
        </div>
      </div>
    </div>
  )
}

export default function Chapter() {
  return (
    <>
      <h1>{frontmatter.title}</h1>

      <LanguageSelector defaultLang={typeof window !== 'undefined' ? (localStorage.getItem('book_lang') || 'en') : 'en'} />

      ## Signup with Better-Auth

      Better-Auth provides a comprehensive authentication solution for the book application:

      - **Multi-Provider Support**: Email/password, OAuth (Google, GitHub, etc.)
      - **Session Management**: Secure JWT-based session handling
      - **Database Integration**: Works with any database system
      - **Type Safety**: Full TypeScript support with generated types

      ### Installation

      ```bash
      npm install better-auth
      ```

      ### Configuration

      ```typescript
      import { betterAuth } from "better-auth";
      import { neon } from "@neondatabase/serverless";

      export const auth = betterAuth({
        database: neon(process.env.DATABASE_URL!),
        socialProviders: {
          google: {
            clientId: process.env.GOOGLE_CLIENT_ID!,
            clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
          },
        },
        emailAndPassword: {
          enabled: true,
          requireEmailVerification: true,
        },
      });
      ```

      ## Gathering User Background

      Understanding user background enables personalized content delivery:

      - **Technical Experience**: Beginner, intermediate, expert levels
      - **Domain Knowledge**: Robotics, AI, software development, etc.
      - **Hardware Access**: Available equipment and platforms
      - **Learning Goals**: Specific objectives and use cases

      ### Background Collection Form

      The signup flow includes a comprehensive background survey:

      1. **Experience Level**: Years of experience in relevant fields
      2. **Technical Skills**: Programming languages, frameworks, tools
      3. **Hardware Access**: Current robotics/hardware setup
      4. **Learning Objectives**: Specific goals and interests
      5. **Industry Focus**: Academic, hobbyist, professional, etc.

      ## Dynamic Chapter Personalization

      Personalization adapts content based on user profile and preferences:

      - **Content Filtering**: Show/hide advanced topics based on skill level
      - **Example Customization**: Use relevant examples based on user interests
      - **Pathway Guidance**: Suggest optimal learning paths
      - **Difficulty Adjustment**: Modify complexity based on user background

      ### Implementation Approach

      1. **Profile Analysis**: Analyze user background data
      2. **Content Tagging**: Tag content with difficulty and prerequisites
      3. **Adaptive Rendering**: Show personalized content variants
      4. **Feedback Loop**: Adjust recommendations based on user engagement

      ## One-Click Translation Buttons

      Translation features enable global accessibility:

      - **Real-time Translation**: Convert content on-demand
      - **Multiple Languages**: Support for Urdu, Arabic, German, etc.
      - **Quality Preservation**: Maintain technical accuracy during translation
      - **User Preference**: Remember translation preferences

      ### Translation Pipeline

      ```
      User Request → Language Detection → Translation Service → Content Delivery
      ```

      ### Images

      ![Better-Auth Integration](static/img/personalization/better_auth_integration.png)

      ![User Profile Management](static/img/personalization/user_profile_management.png)

      ### Multilingual Section

      <div id="multilingual-section">

      <div data-language="en">

      ## Multilingual Section — English

      This is the English translation and original content for the Personalization chapter.

      </div>

      <div data-language="ur">

      ## Multilingual Section — اردو

      یہ Personalization کا باب کا اردو ترجمہ ہے۔

      </div>

      <div data-language="ru">

      ## Multilingual Section — Roman Urdu

      Ye Personalization ka Roman Urdu tarjuma hai.

      </div>

      <div data-language="ar">

      ## Multilingual Section — العربية

      هذا هو ترجمة فصل Personalization إلى العربية.

      </div>

      <div data-language="de">

      ## Multilingual Section — Deutsch

      Dies ist die deutsche Übersetzung des Personalization Kapitels.

      </div>

      </div>

    </>
  )
}