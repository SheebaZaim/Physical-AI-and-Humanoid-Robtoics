---
title: 'Appendices, Diagrams & Resources'
sidebar_label: 'Appendices & Resources'
slug: '/appendices_resources'
---

import React from 'react'

export const frontmatter = {
  title: 'Appendices, Diagrams & Resources',
  sidebar_label: 'Appendices & Resources',
  slug: '/appendices_resources'
}

// LanguageSelector component: minimal, accessible, and client-side only.
// Place this at the top of each chapter .mdx (or import it from a shared component file).

export function LanguageSelector({ defaultLang = 'en' }) {
  const [lang, setLang] = React.useState(defaultLang)

  React.useEffect(() => {
    // keep selection in localStorage so user choice persists across pages
    try { localStorage.setItem('book_lang', lang) } catch (e) {}
  }, [lang])

  return (
    <div className="language-selector" aria-label="Choose language" style={{ marginBottom: '1rem' }}>
      <div style={{ display: 'flex', gap: '0.5rem', flexWrap: 'wrap' }}>
        <button onClick={() => setLang('en')} aria-pressed={lang === 'en'}>English</button>
        <button onClick={() => setLang('ur')} aria-pressed={lang === 'ur'}>اردو</button>
        <button onClick={() => setLang('ru')} aria-pressed={lang === 'ru'}>Roman Urdu</button>
        <button onClick={() => setLang('ar')} aria-pressed={lang === 'ar'}>العربية</button>
        <button onClick={() => setLang('de')} aria-pressed={lang === 'de'}>Deutsch</button>
      </div>

      <style>{`
        .language-selector button { padding: 0.45rem 0.7rem; border-radius: 6px; border: 1px solid rgba(0,0,0,0.08); background: transparent; cursor: pointer; }
        .language-selector button[aria-pressed="true"] { box-shadow: 0 0 0 3px rgba(0,0,0,0.04) inset; font-weight: 600; }
      `}</style>

      {/* Hidden inputs to allow CSS-only targeting if desired */}
      <input type="hidden" id="selected-lang" value={lang} />

      {/* Render translations container statefully */}
      <div>
        {/* The translation blocks below are always present in the .mdx (for RAG / offline retrieval),
            but they include a data-language attribute and are hidden unless selected. */}
        <div data-language="en" style={{ display: lang === 'en' ? 'block' : 'none' }}>
          {/* English content will appear here (can duplicate main content or provide summary). */}
        </div>
        <div data-language="ur" style={{ display: lang === 'ur' ? 'block' : 'none' }}>
          {/* Urdu translation block */}
        </div>
        <div data-language="ru" style={{ display: lang === 'ru' ? 'block' : 'none' }}>
          {/* Roman Urdu translation block */}
        </div>
        <div data-language="ar" style={{ display: lang === 'ar' ? 'block' : 'none' }}>
          {/* Arabic translation block */}
        </div>
        <div data-language="de" style={{ display: lang === 'de' ? 'block' : 'none' }}>
          {/* German translation block */}
        </div>
      </div>
    </div>
  )
}

export default function Chapter() {
  return (
    <>
      <h1>{frontmatter.title}</h1>

      <LanguageSelector defaultLang={typeof window !== 'undefined' ? (localStorage.getItem('book_lang') || 'en') : 'en'} />

      ## Robotics Glossary

      A comprehensive collection of terms used throughout the book:

      - **Actuator**: A component that moves or controls a mechanism
      - **End Effector**: The device at the end of a robotic arm designed to interact with the environment
      - **Forward Kinematics**: The use of kinematic equations to compute the position of the end-effector from specified values of joint parameters
      - **Inverse Kinematics**: The mathematical process of calculating the variable joint parameters needed to place the end of a kinematic chain
      - **PID Controller**: Proportional-Integral-Derivative controller for precise motor control
      - **SLAM**: Simultaneous Localization and Mapping
      - **ROS**: Robot Operating System
      - **URDF**: Unified Robot Description Format
      - **VSLAM**: Visual Simultaneous Localization and Mapping
      - **VLA**: Vision-Language-Action system

      ## Command Cheat Sheets

      ### ROS 2 Commands
      ```bash
      # Start ROS 2 daemon
      ros2 daemon start

      # List all topics
      ros2 topic list

      # Echo messages on a topic
      ros2 topic echo /topic_name std_msgs/String

      # Run a node
      ros2 run package_name node_name

      # Launch a launch file
      ros2 launch package_name launch_file.launch.py
      ```

      ### Isaac Sim Commands
      ```bash
      # Start Isaac Sim
      python -m omni.isaac.kit --exts omni.isaac.examples

      # Export USD files
      python scripts/export_usd.py --input_scene scene.usd --output_path=output/
      ```

      ### Git Commands for Collaboration
      ```bash
      # Clone the repository
      git clone https://github.com/PhysicalAIProject/book.git

      # Create a new branch for your work
      git checkout -b feature/new-chapter

      # Commit your changes
      git add .
      git commit -m "Add new chapter content"

      # Push to remote
      git push origin feature/new-chapter
      ```

      ## Hardware Setup Guides

      ### Jetson Orin Nano Setup
      1. Flash JetPack SDK using SDK Manager
      2. Connect power, monitor, keyboard, and mouse
      3. Complete initial setup wizard
      4. Install ROS 2 Humble Hawksbill
      5. Set up development environment

      ### RealSense D435i Setup
      1. Connect via USB 3.0 cable
      2. Install RealSense SDK
      3. Test with realsense-viewer
      4. Calibrate depth sensor
      5. Integrate with ROS 2

      ## Study Roadmap

      ### Beginner Track
      1. Introduction to Physical AI
      2. Robotics Foundations
      3. Basic ROS 2 Concepts
      4. Simple Simulation with Gazebo
      5. Basic Navigation

      ### Intermediate Track
      1. ROS 2 Nervous System
      2. Digital Twin Concepts
      3. Isaac AI-Robot Brain
      4. Vision-Language-Action Systems
      5. Hardware Integration

      ### Advanced Track
      1. Sim-to-Real Transfer
      2. Cloud vs On-Premise
      3. Advanced Perception
      4. Multi-Robot Systems
      5. Reusable Intelligence

      ## Additional Resources

      - [ROS 2 Documentation](https://docs.ros.org/en/humble/)
      - [NVIDIA Isaac Sim Documentation](https://docs.omniverse.nvidia.com/isaacsim/latest/)
      - [Docusaurus Documentation](https://docusaurus.io/docs)
      - [Qdrant Vector Database](https://qdrant.tech/documentation/)
      - [Better-Auth Documentation](https://better-auth.com/docs)
      - [GitHub Repository](https://github.com/PhysicalAIProject/book)

      ### Images

      ![Robotics Components Diagram](static/img/appendices_resources/robotics_components.png)

      ![System Architecture](static/img/appendices_resources/system_architecture.png)

      ### Multilingual Section

      <div id="multilingual-section">

      <div data-language="en">

      ## Multilingual Section — English

      This is the English translation and original content for the Appendices & Resources chapter.

      </div>

      <div data-language="ur">

      ## Multilingual Section — اردو

      یہ Appendices & Resources کا باب کا اردو ترجمہ ہے۔

      </div>

      <div data-language="ru">

      ## Multilingual Section — Roman Urdu

      Ye Appendices & Resources ka Roman Urdu tarjuma hai.

      </div>

      <div data-language="ar">

      ## Multilingual Section — العربية

      هذا هو ترجمة فصل Appendices & Resources إلى العربية.

      </div>

      <div data-language="de">

      ## Multilingual Section — Deutsch

      Dies ist die deutsche Übersetzung des Appendices & Resources Kapitels.

      </div>

      </div>

    </>
  )
}